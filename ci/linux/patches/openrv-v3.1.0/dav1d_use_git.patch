diff --git a/cmake/dependencies/dav1d.cmake b/cmake/dependencies/dav1d.cmake
index 4f5082a..7bf6e55 100644
--- a/cmake/dependencies/dav1d.cmake
+++ b/cmake/dependencies/dav1d.cmake
@@ -91,11 +91,10 @@ EXTERNALPROJECT_ADD(
   ${_target}
   DOWNLOAD_NAME ${_target}_${_version}.zip
   DOWNLOAD_DIR ${RV_DEPS_DOWNLOAD_DIR}
-  DOWNLOAD_EXTRACT_TIMESTAMP TRUE
   SOURCE_DIR ${RV_DEPS_BASE_DIR}/${_target}/src
   INSTALL_DIR ${_install_dir}
-  URL ${_download_url}
-  URL_MD5 ${_download_hash}
+  GIT_REPOSITORY "https://github.com/videolan/dav1d.git"
+  GIT_TAG "${_version}"
   CONFIGURE_COMMAND ${_configure_command} ./_build --libdir=${_lib_dir_name} --default-library=${_default_library} --prefix=${_install_dir} -Denable_tests=false
                     -Denable_tools=false
   BUILD_COMMAND ${_make_command} -C _build
